# Open Food Facts API
# Required for API requests. Format: AppName/Version (ContactEmail)
# Server-only (no NEXT_PUBLIC_ prefix). Open Food Facts throttles/blocks the
# generic placeholder — set a real contact before deploying.
OPENFOODFACTS_USER_AGENT=SnackScout/1.0 (hi@matiasmariani.io)

# Analytics (optional)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=